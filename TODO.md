# TODO

## Context

Rebuilding the GitHub profile README (`allcapsjoe/ALLCAPSJOE`) to use a neon CRT-style SVG banner
at the top, with the existing markdown content in a collapsed `<details>` block below, and
auto-updating feed sections beneath that.

The SVG is generated by the sibling repo `readme-console-generator` (local: `C:/develop/ALLCAPS/readme-console-generator`).

---

## Done

- [x] Created `assets/readme-banner.svg` — slim CRT-style banner (title bar, three dots, prompt, cursor). Static, hand-crafted.
- [x] Updated `README.md` — banner SVG at top, all existing content in `<details><summary>read in plain text</summary>`
- [x] Fixed `readme-console-generator` — `_sanitize_line` now preserves internal spaces; text nodes now include `xml:space="preserve"`
- [x] Tests passing in `readme-console-generator`

---

## Up Next

### 1. Add `published` dates to tool + site data files ✅
- Added `published: YYYY-MM-DD` to all 7 tool files and 16 site files
- Used git first-commit dates

### 2. Add ATOM feed generation to `tools-and-sites` ✅
- Added `writeFeed()` to `scripts/generate.mjs`
- Outputs `public/tools/feed.xml` and `public/sites/feed.xml`
- ATOM format, top 10 entries sorted newest first

### 3. Add ATOM feed to `games.allcapsjoe.com` ✅
- Added `scripts/generate-feed.mjs`
- Hooked into `scripts/postbuild.mjs`
- Outputs `dist/feed.xml`

### 4. Confirm blog feed ✅
- Already confirmed working (`blog.allcapsjoe.com/feed/atom.xml`)
- `update-reading-log.mjs` was already consuming it successfully

### 5. Update `README.md` with dynamic feed sections ✅
- Added TOOLS_START/END, GAMES_START/END, SITES_START/END marker blocks

### 6. Write GH Action + feed parser in `allcapsjoe` ✅
- Replaced `update-reading-log.yml` with unified `Update Feeds` workflow
- Added `scripts/update-feeds.mjs` — fetches all 4 feeds, updates all 4 marker blocks

---

## Deferred / Future

- Open-source `readme-console-generator` (make repo public, enable PyPI Trusted Publishing) — see `TODO.md` in that repo
- The full-page SVG idea was explored and shelved — GitHub strips CSS so dark backgrounds can't be applied to markdown. Banner-only SVG is the right call.
- `console-config.base.json` and `readme-console.svg` in `assets/` were generated during exploration but are not currently used in the README. Can be deleted or repurposed later.
