# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a GitHub profile repository (`allcapsjoe/ALLCAPSJOE`). The `README.md` in the root is what appears on the GitHub profile page at github.com/allcapsjoe.

## Structure

- `README.md` — GitHub profile README rendered on the public profile page. Supports standard GitHub Flavored Markdown and HTML.

## Development Notes

- There are no build steps, tests, or dependencies — this repo is purely content.
- Changes to `README.md` on the `main` branch are immediately reflected on the GitHub profile page.
- The `claude` and `codex` branches exist for AI-assisted edits before merging to `main`.
